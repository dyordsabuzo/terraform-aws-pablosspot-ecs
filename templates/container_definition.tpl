{
    "name" : "${container_name}",
    "image" : "${image}",
    "cpu": ${cpu},
    "memory": ${memory},
    "essential": ${essential},
    "environment": ${environment},
    "secrets": ${secrets},
    "portMappings": ${port_mappings},
    "logConfiguration": ${log_configuration}
}